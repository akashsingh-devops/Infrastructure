AWSTemplateFormatVersion: '2010-09-09'
Description: AWS CloudFormation Template for creating an ec2 

Parameters:
  Environment:
    Type: String
    AllowedValues:
      - dev
      - prod
      - qa
  ImageId:
    Type: String
    Description: AMI ID to create the EC2 Instances

Mappings:
  EnvMap:
    dev:
      instanceType: "t2.micro"
      name: "dev"
    qa:
      instanceType: "t2.micro"
      name: "qa"
    prod:
      instanceType: "t2.micro"
      name: "prod"


Conditions:
  CreateProdResources: !Equals [ !Ref Environment, prod ]

Resources:
  Instance:
    Type: AWS::EC2::Instance
    Properties:
      InstanceType: !FindInMap [EnvMap, !Ref Environment, instanceType]
      ImageId: !Sub ImageId
      Tags:
        - Key: "Name"
          Value: !FindInMap [EnvMap, !Ref Environment, name]
  
  MountPoint:
    Type: AWS::EC2::VolumeAttachment
    Condition: CreateProdResources
    Properties:
      InstanceId: !Ref Instance
      VolumeId: !Ref NewVolume
      Device: /dev/sdh

  NewVolume:
    Type: AWS::EC2::Volume
    Condition: CreateProdResources
    Properties:
      Size: 5
      AvailabilityZone: !GetAtt Instance.AvailabilityZone

Outputs:
  InstanceName:
    Value: !Ref Instance
